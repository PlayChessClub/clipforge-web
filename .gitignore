# 构建产物(由 build-pkgs.py 产出,二进制应发到 GitHub Releases,不进源码库)
release/

# 交叉编译中间体(build-pkgs.py 在仓库根生成)
/clipforge-*

# 本地密钥(含主密钥与密文配置文件,绝不入库)
master.key
settings.yml

# Android 壳构建产物(android/build.sh 产出; APK 发到 Releases, 不进源码库)
android/build/
android/out/
android/keystore/
*.apk
.DS_Store

# GitHub 令牌本地文件（绝不入库）
ghkey.yml

# 自测中间产物
selftest/.records.jsonl
